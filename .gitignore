# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit real secrets
.env
.env.local
.env.production
.env.staging

# Firebase service account — NEVER commit this — grants full Admin SDK access
firebase-service-account.json
*service-account*.json
*serviceAccount*.json

# Claude — personal/local settings stay local; shared config is .claude/settings.json
.claude/settings.local.json
# Backup ZIPs used for seeding (may contain real data) — keep out of git
backups/
*.backup.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
